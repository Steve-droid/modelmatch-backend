.git
.venv
__pycache__
*.pyc
.pytest_cache
.mypy_cache
.ruff_cache
tests
docs
# NOTE: `agent/` is intentionally NOT ignored — the agent image (agent/Dockerfile)
# COPYs it. The backend image doesn't COPY it, so it's harmless in that context.
*.md
.env
.env.*
!.env.example
