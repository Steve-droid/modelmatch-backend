<!-- FIXTURE / DEMO SOURCE — synthetic text for ModelMatch ingestion tests. NOT a real cited benchmark; all figures are invented. -->

# Incomplete rows (FIXTURE)

Rows missing required fields (no metric+score; no score). The validator must drop
both — proving it never fabricates a missing metric or score.
