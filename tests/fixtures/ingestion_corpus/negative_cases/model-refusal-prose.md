<!-- FIXTURE / DEMO SOURCE — synthetic text for ModelMatch ingestion tests. NOT a real cited benchmark; all figures are invented. -->

# Ambiguous notes (FIXTURE)

Free text the model can't turn into clean JSON — it replies in prose instead of
the required envelope. The validator must reject the whole (non-JSON) output.
