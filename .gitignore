# Node (vendored Playwright E2E smoke under ci/e2e — only package.json + the lockfile are tracked)
node_modules/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# uv
.uv/

# Env / secrets
.env
.env.*
!.env.example

# Alembic local
*.sqlite3

# Local demo/reference data copied from tracked fixtures/seeds.
# Not part of the application image or source-controlled artifact set.
data/*
# Exception (P38c): data/catalog/ holds the PINNED upstream benchmark sources the
# deterministic RealVuln ingest reads and its tests load at import time, so they ARE
# source. Not shipped in the image — the container seeds from app/catalog/seed_data.json.
!data/catalog/

# OS / editor
.DS_Store
.idea/
.vscode/

# Browser smoke-test artifacts
test-results/
